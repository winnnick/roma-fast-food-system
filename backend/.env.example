# Configuración local de referencia. No guardar secretos reales en Git.
NODE_ENV=development
CORS_ORIGINS=http://localhost:5173

# PostgreSQL (cuando se ejecuta desde Docker Compose, cada servicio recibe su propia URL)
DATABASE_URL=postgresql://roma:roma_dev_password@localhost:55432/roma_auth
DATABASE_SSL=false

# RabbitMQ
RABBITMQ_ENABLED=false
RABBITMQ_URL=amqp://roma:roma_dev_password@localhost:5672
RABBITMQ_QUEUE=roma.auth
